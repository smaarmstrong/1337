// a function named main; main is the first code that runs in every executable Rust program
fn main() {
    println!("Hello, world!"); // ! indicates a macro not a function
}
// compile with `rustc main.rs`
// execute with `./main`
