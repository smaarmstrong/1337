cargo new get-dependencies
cd get-dependencies
cargo add rand@0.8.5 trpl@0.2.0
# you can delete get-dependencies after adding the packages
# run --offline with cargo commands to use cached versions instead
